#include <stdio.h>

/**
 * main - writes a program that prints a string
 *
 * @printf - the function used to print the string
 *
 * Return: 0 on success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
