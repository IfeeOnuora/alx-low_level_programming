#include "main.h"

/**
 * print_number - a function that prints only integer
 *
 * @n: the integer to be printed
 *
 * Return: Always 0.
 */
void print_number(int n)
{
	unsigned int c;

	if (n <= 0)
	{
		c = -n
		_putchar(n + '0');
	}
	else if (n !=(n > 0 && n <= 47))
	{
		_putchar(n + '0');
	}
	else if (n !=(n >= 58 && n <= 127))
	{
		_putchar(n + '0');
	}	
}
